/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { TimeMsPipe } from './time-ms.pipe';

describe('TimeMsPipe', () => {
  it('create an instance', () => {
    let pipe = new TimeMsPipe();
    expect(pipe).toBeTruthy();
  });
});
